
.DS_Store
.claude/settings.local.json

infra/terraform/infra-outputs.json

# --- ai-dlc-bootstrap managed ---
node_modules/
package-lock.json
dist/
tsconfig.base.json
# --- end ai-dlc-bootstrap managed ---

# Helm dependency build artefacts (regenerated by `helm dependency build`)
**/helm/charts/
**/helm/Chart.lock

# Terraform — provider cache + binary plans (regenerated by `terraform init`)
**/.terraform/
**/.terraform.lock.hcl
**/plan.bin
**/*.tfstate
**/*.tfstate.*
**/*.tfplan

# Python virtualenvs + caches
**/.venv/
**/__pycache__/
**/.pytest_cache/
**/.hypothesis/
**/.mypy_cache/
**/.ruff_cache/

# Go build artefacts
**/bin/
harness/local-test-rig/local-test-rig

# Vite demo build output (dev only; library build goes to dist/)
**/dist-demo/

# Editor + OS junk
*.swp
*.swo
.idea/
.vscode/

# Logs + coverage
*.log
coverage/
.coverage
*.cov
