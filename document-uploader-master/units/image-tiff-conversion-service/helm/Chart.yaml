apiVersion: v2
name: image-tiff-conversion-service
description: Image and TIFF route — sharp + PDFKit (ranged geotiff reads for TIFF via COG)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: docuploader-chassis
    version: 0.1.0
    repository: file://../../platform-network-and-compute/helm/docuploader-chassis
