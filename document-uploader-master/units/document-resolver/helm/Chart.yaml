apiVersion: v2
name: document-resolver
description: Document gRPC resolver — documents table + presigned URL minting + idempotency + statusChanged subscription resolver
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: docuploader-chassis
    version: 0.1.0
    repository: file://../../platform-network-and-compute/helm/docuploader-chassis
