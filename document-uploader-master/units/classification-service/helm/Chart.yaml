apiVersion: v2
name: classification-service
description: Pre-routing classifier — magic-byte detection via file-type, route dispatch via SQS
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: docuploader-chassis
    version: 0.1.0
    repository: file://../../platform-network-and-compute/helm/docuploader-chassis
